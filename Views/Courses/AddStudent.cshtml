@{
  Layout = "_Layout";
}

@model University.Models.Course

<h2>Add a Student</h2>

<h4>Add a Student to this task: @Html.DisplayFor(model => model.CourseName)</h4>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.CourseId)

    @Html.Label("Select Student")
    @Html.DropDownList("StudentId")

    <input type="submit" value="Save" />
}

<button>@Html.ActionLink("Back to list", "Index")</button>